# Merkle Tree

Merkle tree data structure using SHA-256 — the foundation of blockchain integrity.

## Features
- Build a tree from arbitrary data blocks
- Compute the Merkle root
- Generate and verify inclusion proofs

## Modules
- `tree.rs` — node structure, tree construction, proof generation/verification
- `main.rs` — CLI demo: build tree, print root, verify a leaf
