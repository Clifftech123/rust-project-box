# AES Cipher

AES-256 symmetric encryption and decryption from scratch.

## Modules
- `aes.rs` — core AES operations: SubBytes, ShiftRows, MixColumns, AddRoundKey
- `modes.rs` — block cipher modes: ECB and CBC
- `main.rs` — CLI: encrypt/decrypt a file or string with a key
