# UDP Server/Client

UDP datagram server and client.

## Modules
- `server.rs` — binds a UDP socket and handles datagrams
- `client.rs` — sends datagrams and waits for responses
- `main.rs` — entry point (select server or client mode)
