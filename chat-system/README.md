# Chat System

Multi-client TCP chat system.

## Modules
- `server.rs` — handles multiple clients concurrently
- `client.rs` — connects and sends/receives messages
- `main.rs` — entry point (run as server or client)
