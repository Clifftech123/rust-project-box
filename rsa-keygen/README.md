# RSA Keygen

RSA asymmetric key-pair generation, encryption, and decryption.

## Modules
- `keygen.rs` — prime generation, public/private key derivation
- `crypto.rs` — modular exponentiation, encrypt/decrypt
- `main.rs` — CLI: generate keys, encrypt, decrypt
