# Caesar Cipher

Classical Caesar shift cipher with a brute-force cracker.

## Modules
- `cipher.rs` — encrypt, decrypt, and brute-force all 25 shifts
- `main.rs` — CLI: choose mode (encrypt / decrypt / crack) and shift value
