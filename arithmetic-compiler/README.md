# Arithmetic Compiler

Compiles arithmetic expressions into assembly-like instructions.

## Modules
- `tokenizer.rs` — breaks input into tokens
- `parser.rs` — builds an AST from tokens
- `assembly.rs` — generates code from the AST
- `main.rs` — CLI entry point
