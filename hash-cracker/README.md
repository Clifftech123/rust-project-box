# Hash Cracker

Dictionary and brute-force hash cracker.

## Supported hashes
- MD5, SHA-1, SHA-256, SHA-512

## Modules
- `cracker.rs` — dictionary attack, brute-force with charset/length config
- `main.rs` — CLI: target hash, hash type, wordlist or brute-force mode
