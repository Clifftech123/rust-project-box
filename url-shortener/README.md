# URL Shortener

Generates short aliases for long URLs with in-memory or file-backed storage.

## Modules
- `shortener.rs` — hash-based key generation and store lookup
- `main.rs` — CLI or HTTP interface
