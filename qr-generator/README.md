# QR Generator

Generates QR codes and writes them as PNG images.

## Modules
- `qr_code.rs` — QR encoding logic (data matrix)
- `png_writer.rs` — writes the matrix to a PNG file
- `main.rs` — CLI entry point
