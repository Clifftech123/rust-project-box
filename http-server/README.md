# HTTP Server

Simple HTTP/1.1 server that serves static files.

## Modules
- `server.rs` — request parsing and response building
- `main.rs` — entry point and listener setup

## Static Files
- `static/index.html` — default served page
