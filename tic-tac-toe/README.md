# Tic-Tac-Toe

Networked tic-tac-toe — two players connect over TCP.

## Modules
- `server.rs` — game host, board state, turn management
- `client.rs` — connects to server, renders board, sends moves
- `main.rs` — run as server or client
